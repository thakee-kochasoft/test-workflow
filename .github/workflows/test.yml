
name: Test

on:
  workflow_dispatch:
    inputs:
      input_1:
        default: "hello"

jobs:
  test-run:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3

      - name: S1
        run : |
          echo $GITHUB_REPOSITORY
          python -c 'print("$GITHUB_REPOSITORY".split("/"))'
