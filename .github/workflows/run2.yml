
name: CI2
on: [workflow_dispatch]

jobs:
  test-run:
    runs-on: windows-latest
    steps:
    
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Run Test
        shell: cmd
        run: |
          echo Hello world
